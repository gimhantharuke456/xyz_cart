import 'package:flutter/material.dart';
import 'package:xyz_cart/utils/app_colors.dart';

ThemeData get appTheme => ThemeData(
      scaffoldBackgroundColor: secondaryColor,
      primaryColor: primaryColor,
    );
